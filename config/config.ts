const host = 'http://localhost:3000';
const config = {
    host: host,
    api: host + '/api',
    operationType: {
        expense: 'expense',
        income: 'income'
    }
}

export default config;