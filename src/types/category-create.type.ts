export type CategoryCreateType = {
    type: string|null,
    category_id: number|null,
    amount: number|null,
    date: string | Date|null,
    comment: string|null,
}

export type CreateClass = {
    title: string
}